[buildout]
parts = django-1.7
develop = ..
eggs = 
  django-piston
  pyyaml
versions = versions

[versions]
Django = 1.7

[django-1.7]
recipe = djangorecipe
project = test_project
settings = settings
test = testapp
eggs = ${buildout:eggs}
testrunner = test-1.7
